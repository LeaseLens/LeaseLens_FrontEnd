export interface BannerProps {
    bannerTxt: string;
    onClick?: () => void;
}

export interface ProProps {
    width: string;
    height: string;
}

export interface PostProps {
    fontSize: string;
}